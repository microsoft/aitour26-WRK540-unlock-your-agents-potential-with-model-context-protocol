{
    "configurations": [
        {
            "type": "aspire",
            "request": "launch",
            "name": "🔁🤖Debug Aspire",
            "program": "${workspaceFolder}"
        }
    ]
}